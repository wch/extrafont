This directory is where extracted afm files are stored.

The file `afm_table.csv` is generated by `afm_generate_table()`. It contains
information about the afm files, including the FontName, FontFamily, and
FullName.
